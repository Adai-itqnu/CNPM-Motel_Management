import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-bill-management',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './bill-management.component.html',
  styleUrl: './bill-management.component.css'
})
export class BillManagementComponent {
  // Quản lý hóa đơn
  // Xem danh sách hóa đơn hàng tháng
  // Nhập chỉ số điện, nước để chốt hóa đơn
  // Tạo hóa đơn mới
}
